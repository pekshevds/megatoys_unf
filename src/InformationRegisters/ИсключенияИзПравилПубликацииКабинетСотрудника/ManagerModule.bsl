#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда

#Область СлужебныеПроцедурыИФункции

Процедура ЗарегистрироватьОтветственного(ТекущийОтветственный, НовыйОтветственный) Экспорт

	ФизическиеЛица = Новый Массив;
	
	Если ЗначениеЗаполнено(ТекущийОтветственный) Тогда
		Запись = РегистрыСведений.ИсключенияИзПравилПубликацииКабинетСотрудника.СоздатьМенеджерЗаписи();
		Запись.ФизическоеЛицо = ТекущийОтветственный;
		Запись.Удалить();
		ФизическиеЛица.Добавить(ТекущийОтветственный);
	КонецЕсли;
	
	Если ЗначениеЗаполнено(НовыйОтветственный) Тогда
		Запись = РегистрыСведений.ИсключенияИзПравилПубликацииКабинетСотрудника.СоздатьМенеджерЗаписи();
		Запись.ФизическоеЛицо = НовыйОтветственный;
		Запись.Публикуется 		= Истина;
		Запись.ЭтоОтветственный = Истина;
		Запись.Записать();
		Физическиелица.Добавить(НовыйОтветственный);
	КонецЕсли;
	
	КабинетСотрудника.ОбновитьСведенияОПубликацииФизическихЛиц(ФизическиеЛица);

КонецПроцедуры

#КонецОбласти

#КонецЕсли
