#Область ОбработчикиСобытийФормы

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	ДанныеТаблицыПолучателей = ПолучитьИзВременногоХранилища(Параметры.АдресТаблицыУчастников);
	
	Для Каждого Получатель Из ДанныеТаблицыПолучателей Цикл
		НовыйПолучатель = ТаблицаПолучателей.Добавить();
		ЗаполнитьЗначенияСвойств(НовыйПолучатель, Получатель);
	КонецЦикла;
	
	Заголовок = СтрШаблон(НСтр("ru = 'Получатели: %1'"), ТаблицаПолучателей.Количество());
	
КонецПроцедуры

#КонецОбласти