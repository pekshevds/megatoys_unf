#Область ПрограммныйИнтерфейс

//  Устанавливает значение константы
//
// Параметры:
//  ЗначениеХранения - Булево.
//
Процедура УстановитьЛокальноеХранениеКарт(ЗначениеХранения) Экспорт
	
	УстановитьПривилегированныйРежим(Истина);
	ТекущееЗначение = Константы.ЛокальноеХранениеДисконтныхКарт.Получить();
	Если Не ТекущееЗначение = ЗначениеХранения Тогда
		Константы.ЛокальноеХранениеДисконтныхКарт.Установить(ЗначениеХранения);
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти